[x] make drawText wrap text when too long
[x] make functions return ints, not vectors to declutter functions calls when sending messages
    - turtle/GPS/handleCoordsInput
    - turtle/GPS/locate
    - server/GPS/handleCoordsInput
    - server/GPS/locate
[x] send recall point or "y" to turtle for first mining arg
[x] de-index caller args for turtle init
[x] change all occurances of getHeading() for setHeading()
[~] (consider changing calls to turtle.turn*() to custom GPS.turn*() [or steer*()] function to better manage heading)
[~] make it so main menu writes "Disconnect" when there is a turtle connected
[~] change the completion bar to display background colour instead of I's
[ ] put parallel functions where time is important e.g. ping timeout
[ ] automatically disconnect turtle if main script errors (prob with a multishell)
[ ] make a settings menu on turtle (-s flag) and server side (menu option)
[ ] make turtle pathfinds around inventory blocks and CC blocks
[ ] (make it so user doesn't have to input coordinates if they haven't changed)
[ ] Implement inventory sorting and retrieval
[ ] Add more mining patterns such as stairs, tunnel, quarry