{
  pingTimeOut = {
    caption = "Ping Timeout",
    value = 4,
    desc = "Pinging turtles will timeout after not receiving pings for this amount of time (sec)."
    },
    inventorySets = {
      caption = "Inventory Sets",
      value = {},
      desc = "Groups of items that cannot be retrieved individually, only as a set. The take the form {'item_name', 'peripheral_name', slot_num}"
    },
    defaultValues = {
      caption = "Default Values",
      value = {},
      desc = "Values used by default by computer. Mainly used for debugging."
    }
}